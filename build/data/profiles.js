"use strict";

/* Per-entity distinctive facts. These drive the differentiated fallback
   composer so every region page describes genuinely different neighborhoods,
   stations and venue mixes (not a name-swapped template). Authored content
   files may override any entry with fuller prose. */

const PROFILES = {
  // Content authored/expanded by build-content agents into ./content/*.js.
  // This module seeds distinctive facts; keys are registry slugs.
};

module.exports = { PROFILES };
